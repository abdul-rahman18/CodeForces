#include <bits/stdc++.h>
using namespace std;
 
int main() {
	// your code goes here
    int t; cin >> t;
    while(t--)
    {
        int n,k; cin >> n >> k;
        
        for(int i=0;i<n;i++)
        {
            int x; cin >> x;
            
            if(x == k) k = -1;
        }
        
        if(k==-1) cout << "YES" << endl;
        else cout << "NO" << endl;
    }
}